export declare class RetryScheduledEvent {
    readonly correlationId: string;
    readonly attempt: number;
    readonly delayMs: number;
    constructor(correlationId: string, attempt: number, delayMs: number);
}
//# sourceMappingURL=retry-scheduled.event.d.ts.map